
var React = window.React = require('react'),
    ReactDOM = require("react-dom"),
    mountNode = document.getElementById("app"),
    FormApp = require("./components/FormApp");


ReactDOM.render(<FormApp />, mountNode);